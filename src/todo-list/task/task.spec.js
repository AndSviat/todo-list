import { Task } from './task';

const expect = require('chai').expect;

describe('Task', function() {
    it('should create a task object', function() {
        expect(true).to.be.a('boolean');
    })
});
